project for go-gin-web


docker化本地mock数据，设计相应的构建流程：


标准化本地打包测试流程：
1.使用dockerfile进行镜像构建的配置
2.构建镜像：docker build -t sayhello .
3.【配置docker-compose.yml文件】并使用compose进行启动容器：
docker-compose -f docker-compose.yml up -d world

test ok
gin整体框架实践【外加微服务基本框架】